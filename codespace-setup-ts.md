### Install typescript
npm install -g typescript # install ts
tsc --init # initialize ts

npx run dev # to run the server at 3000
npx tsx src/index.ts # to run the ts file

Run the generated JavaScript file:
node dist/index.js

